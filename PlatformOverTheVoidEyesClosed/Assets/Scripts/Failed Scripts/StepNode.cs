using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StepNode : ProgressionNode
{
    /*
    Transform playerTrans = null;

    //which axis is bigger x or z (the player will be moving towards this object in the opposite axis)
    bool horizontal = false;
    private void Start()
    {

        if (transform.localScale.x > transform.localScale.z)
            horizontal = true;
        else
            horizontal = false;
    }
    private bool PlayerPassed()
    {
        if (horizontal && transform.position.z <= )
        {
            transform.position
        }
        return false;
    }
    */
}
